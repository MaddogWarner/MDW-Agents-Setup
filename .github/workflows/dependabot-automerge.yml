name: Dependabot auto-merge

on: pull_request

permissions:
  contents: read

jobs:
  automerge:
    uses: MaddogWarner/.github/.github/workflows/reusable-dependabot-automerge.yml@v1
    permissions:
      contents: write
      pull-requests: write
