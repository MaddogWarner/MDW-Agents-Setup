name: Security

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  security-events: write

jobs:
  security:
    uses: MaddogWarner/.github/.github/workflows/reusable-security.yml@v1
    permissions:
      contents: read
      security-events: write
    with:
      ecosystem: none
