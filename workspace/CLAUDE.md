# Working with David — Workspace-specific Instructions

This file adds **workspace-specific** context on top of Cowork's global instructions, which carry the universal rules (AU English, tone, push-back behaviour, citations, never-delete, work focus, workflow, token efficiency) **and** the references to `aboutme/about-me.md`, `aboutme/memory.md`, and `aboutme/writingstyle.md`.

If the global instructions are ever missing or out of date, the canonical copy lives at `/Users/maddog/Documents/Claudius/cowork-global-instructions.md`.

## File handling (workspace)
- **Workspace folder:** `/Users/maddog/Documents/Claudius/` — save all deliverables here so David can see them.
- Save everything (drafts, code, outputs, documents) into this folder by default.
- Use `computer://` links when sharing files.
